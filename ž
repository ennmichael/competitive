#pragma once

#include <vector>
#include <unordered_map>;

namespace Competitive {

    template <class Node>
    struct Graph {
        using Nodes = std::vector<Node>;

        Nodes nodes;
        std::unordered_map<Node, Node>
    };

}


All prime factors for number N are less than or equal to sqrt(N).
Every non-prime number above sqrt(N) is composed of prime numbers less than or equal to sqrt(N).
This is why in Erathostene's sieve it's good enough for i (outer loop counter) to end at sqrt(N).
